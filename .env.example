# Ryan Memory OS Phase 1 is fully local-first.
# No environment variables or API keys are required.
#
# Future optional provider configuration must remain disabled by default and
# must never be committed with secrets.
