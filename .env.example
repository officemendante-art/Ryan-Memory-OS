# Ryan Memory OS is local-first.
# API keys are optional and must never be committed.
#
# For local developer convenience only:
# VITE_OPENROUTER_API_KEY=replace-with-local-dev-key-only
#
# Prefer the in-app AI Settings page for private local browser use.
# Production/SaaS must use a backend proxy, not a frontend-exposed key.
